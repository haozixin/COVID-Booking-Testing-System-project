package controllers;

import models.Actor;
import views.View;

public class InterviewController extends Controller {
    public InterviewController(View view, Actor actor) {
        super();

    }
}
